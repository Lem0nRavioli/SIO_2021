#include <stdio.h>

int main() {
    int a,b;
    double c,d;
    // printf("enter numbers int: ");
    // scanf("%d %d", &a, &b);
    printf("enter numbers float: ");
    scanf("%lf %lf", &c, &d);

    // printf("%d", a + b);
    printf("%lf", c + d);
}