{
  "name": "npm_cours_2",
  "version": "1.0.0",
  "description": "exercices d entrainement",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=dev nodemon app.js",
    "prod": "cross-env NODE_ENV=prod node app.js",
    "integrateDB": "cross-env mariadb -u root -p < ./database/schemas.sql"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-promise-router": "^4.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
